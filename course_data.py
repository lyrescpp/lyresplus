# ═══════════════════════════════════════════════
#  LYRES COURSE BOT — Контент модулей
# ═══════════════════════════════════════════════

MODULES = {
    "00": {
        "title": "Алмаз-Антей и БПЛА",
        "emoji": "🏭",
        "desc": "Кто это, что делают, где ты будешь работать",
        "lessons": {
            "1": {
                "title": "Что такое Алмаз-Антей",
                "text": """🏭 <b>Концерн ВКО «Алмаз-Антей»</b>

Алмаз-Антей — крупнейший российский оборонный холдинг в области зенитного ракетного оружия и систем ПВО/ПРО. Входит в <b>топ-10 оборонных компаний мира</b>.

<b>Основная продукция:</b>
• С-300, С-400, С-500 — зенитные ракетные комплексы
• Панцирь-С — зенитный пушечно-ракетный комплекс
• Системы управления воздушным пространством
• Радарные комплексы

📍 <b>Структура холдинга:</b>
• <b>НПО «Алмаз»</b> — разработка ЗРС С-400, С-500
• <b>НИИРП</b> — радиолокационные системы
• <b>Концерн «Вега»</b> — БПЛА, разведка, РЭБ
• <b>МНИИПА</b> — автоматизированные системы

🚁 <b>Направление БПЛА:</b>
Активно развивают: разведывательные БПЛА, барражирующие боеприпасы, системы управления роями, бортовые вычислительные комплексы. Именно сюда ты и идёшь."""
            },
            "2": {
                "title": "Что делает программист на БПЛА",
                "text": """💻 <b>Реальные задачи разработчика БПЛА</b>

• <b>Бортовое ПО (firmware)</b> — C++ для микроконтроллеров, управление двигателями, IMU
• <b>Алгоритмы навигации</b> — GPS/ГЛОНАСС, инерциальная навигация, слияние данных датчиков
• <b>Системы управления полётом</b> — PID-регуляторы, стабилизация
• <b>Обработка видео/сигналов</b> — трекинг целей, оптический поток
• <b>Системы связи</b> — протоколы обмена с наземным пунктом
• <b>Тестирование</b> — Software-in-the-loop (SIL), Hardware-in-the-loop (HIL)

🛠️ <b>Стек технологий БПЛА:</b>
<code>C++</code>     — главный язык (ядро системы)
<code>C</code>       — низкоуровневые драйверы
<code>Python</code>  — скрипты, анализ телеметрии
<code>ROS/ROS2</code>— Robot Operating System
<code>Linux RT</code>— ОС реального времени
<code>Git+CMake</code>— сборка и версионирование

⚠️ <b>Специфика:</b>
Ошибка в коде может стоить дорогого железа. Требования к надёжности и документации крайне высокие. Код проходит многоуровневую проверку."""
            },
            "3": {
                "title": "Что нужно знать для старта",
                "text": """📋 <b>Что нужно для трудоустройства</b>

✅ <b>Минимальный набор для старта:</b>
• C++ на уровне классов, указателей, STL
• Базовый Linux (терминал, компиляция, git)
• Понимание что такое микроконтроллер
• Базовая математика: линейная алгебра, тригонометрия

🚀 <b>Что выделит тебя среди других:</b>
• Знание ROS/ROS2 хотя бы базово
• Опыт с Arduino/STM32 (любой проект)
• Понимание PID-регуляторов
• Знание протоколов MAVLink, CAN, UART

💡 <b>Реалистичный путь:</b>
Начнёшь с тестирования и написания утилит. Через 6–12 месяцев — первые задачи на борту. Главное — не бояться задавать вопросы и читать чужой код.

🎯 <b>Следующий шаг:</b>
Переходи к Модулю 01 — начнём изучать C++ с нуля, с примерами из БПЛА."""
            },
        },
        "ai_context": "Ты эксперт по карьере в Алмаз-Антей и разработке БПЛА. Отвечай на русском, кратко и по делу. Собеседник идёт работать программистом на беспилотники в Алмаз-Антей."
    },

    "01": {
        "title": "C++ Основы",
        "emoji": "⚡",
        "desc": "Переменные, типы, условия, циклы — с нуля",
        "lessons": {
            "1": {
                "title": "Первая программа",
                "text": """🖥️ <b>Структура программы на C++</b>

Каждая программа начинается с функции <b>main()</b> — это точка входа.

<pre><code>#include &lt;iostream&gt;
using namespace std;

int main() {
    cout &lt;&lt; "БПЛА ГОТОВ К ВЫЛЕТУ" &lt;&lt; endl;
    return 0;  // 0 = программа завершилась OK
}</code></pre>

📝 <b>Разбор по строкам:</b>
• <code>#include &lt;iostream&gt;</code> — подключаем ввод/вывод
• <code>cout &lt;&lt;</code> — вывод в консоль (console output)
• <code>endl</code> — перенос строки
• <code>return 0</code> — говорим системе «всё ок»

💻 <b>Как запустить:</b>
<pre><code>g++ main.cpp -o program
./program</code></pre>"""
            },
            "2": {
                "title": "Переменные и типы данных",
                "text": """📦 <b>Переменные и типы</b>

Переменная — ячейка памяти с именем. Перед использованием нужно указать <b>тип</b>.

<pre><code>int altitude = 150;       // высота в метрах
double speed = 23.7;      // скорость м/с
bool armed = false;       // вооружён? да/нет
float battery = 87.5f;   // заряд батареи %
char mode = 'A';          // режим полёта

cout &lt;&lt; "Высота: " &lt;&lt; altitude &lt;&lt; " м" &lt;&lt; endl;
cout &lt;&lt; "Скорость: " &lt;&lt; speed &lt;&lt; " м/с" &lt;&lt; endl;</code></pre>

📊 <b>Таблица типов:</b>
<code>int</code>    — целые числа (−2 млрд..+2 млрд)
<code>double</code> — дробные, высокая точность
<code>float</code>  — дробные, меньше памяти
<code>bool</code>   — только true / false
<code>char</code>   — один символ"""
            },
            "3": {
                "title": "Условия if / else",
                "text": """🔀 <b>Условия if / else</b>

В бортовом ПО условия — основа всего: «если батарея &lt; 20% — возврат на базу».

<pre><code>float battery = 15.0f;

if (battery &lt; 10.0f) {
    cout &lt;&lt; "КРИТИЧНО: посадка немедленно!" &lt;&lt; endl;
} else if (battery &lt; 20.0f) {
    cout &lt;&lt; "Возврат на базу (мало заряда)" &lt;&lt; endl;
} else {
    cout &lt;&lt; "Полёт в штатном режиме" &lt;&lt; endl;
}
// Вывод: Возврат на базу (мало заряда)</code></pre>

⚙️ <b>Операторы сравнения:</b>
<code>==</code>  равно         <code>!=</code>  не равно
<code>&gt;</code>   больше        <code>&lt;</code>   меньше
<code>&amp;&amp;</code>  И (оба условия) <code>||</code>  ИЛИ (хоть одно)"""
            },
            "4": {
                "title": "Циклы for / while",
                "text": """🔄 <b>Циклы for / while</b>

Датчики БПЛА отправляют данные сотни раз в секунду. Циклы — обработка потока.

<pre><code>// for — когда знаешь кол-во повторений
for (int i = 0; i &lt; 5; i++) {
    cout &lt;&lt; "Чтение датчика #" &lt;&lt; i &lt;&lt; endl;
}
// i = 0, 1, 2, 3, 4 → 5 итераций</code></pre>

<pre><code>// while — пока условие истинно
int attempts = 0;
int signal = 0;

while (signal == 0 &amp;&amp; attempts &lt; 10) {
    cout &lt;&lt; "Жду GPS... попытка " &lt;&lt; attempts &lt;&lt; endl;
    attempts++;
}

if (signal == 0)
    cout &lt;&lt; "GPS не найден!" &lt;&lt; endl;</code></pre>"""
            },
        },
        "task": {
            "text": """🎯 <b>Задание 01</b>

Напиши программу мониторинга БПЛА:

1. Создай переменные:
   • <code>altitude = 200</code>
   • <code>battery = 45.0</code>
   • <code>armed = false</code>

2. Если <code>altitude &gt; 300</code> → <b>"Превышение высоты!"</b>
3. Если <code>battery &lt; 30</code> → <b>"Мало заряда, возврат"</b>
4. Если <code>armed == false</code> → <b>"Борт невооружён"</b>
5. Иначе → <b>"Всё в норме"</b>

Напиши код или объясни логику 👇""",
            "check_prompt": "Проверь решение задания по C++ для начинающих. Задание: переменные altitude=200, battery=45, armed=false; вывести сообщения по условиям (высота, батарея, вооружение БПЛА). Дай развёрнутый фидбек на русском — что правильно, что улучшить, покажи правильное решение если есть ошибки. Ответ пользователя:\n\n"
        },
        "ai_context": "Ты преподаватель C++ для разработчиков БПЛА. Отвечай на русском, используй примеры из беспилотной авиации и датчиков."
    },

    "02": {
        "title": "Функции",
        "emoji": "🔧",
        "desc": "Разбиваем программу на блоки",
        "lessons": {
            "1": {
                "title": "Синтаксис функций",
                "text": """🔧 <b>Функции — именованные блоки кода</b>

В бортовом ПО БПЛА тысячи строк. Функции позволяют:
• Повторно использовать код
• Разделить задачи между разработчиками
• Тестировать каждую часть отдельно

<pre><code>// тип_возврата имя(параметры) { тело }

double calcDistance(double speed, int time) {
    return speed * time;  // distance = v * t
}

void printStatus(int alt, float bat) {
    cout &lt;&lt; "H=" &lt;&lt; alt &lt;&lt; "м BAT=" &lt;&lt; bat &lt;&lt; "%" &lt;&lt; endl;
}  // void = ничего не возвращает

int main() {
    double d = calcDistance(25.0, 60);
    cout &lt;&lt; "Пройдено: " &lt;&lt; d &lt;&lt; " м" &lt;&lt; endl;
    printStatus(150, 67.5f);
}</code></pre>"""
            },
            "2": {
                "title": "Функция с проверкой безопасности",
                "text": """✅ <b>Функция возвращающая bool</b>

<pre><code>bool isSafeToFly(float battery, int altitude, bool gpsOk) {
    if (battery &lt; 20.0f) return false;
    if (altitude &gt; 500)  return false;
    if (!gpsOk)          return false;
    return true;
}

int main() {
    bool safe = isSafeToFly(75.0f, 200, true);

    if (safe)
        cout &lt;&lt; "Разрешение на вылет выдано" &lt;&lt; endl;
    else
        cout &lt;&lt; "Вылет ЗАПРЕЩЁН" &lt;&lt; endl;
}</code></pre>

💡 <b>Принцип единственной ответственности:</b>
Хорошая функция делает <b>одно дело</b> и делает его хорошо. Это стандарт в ОПК — код должен быть читаемым и проверяемым."""
            },
        },
        "task": {
            "text": """🎯 <b>Задание 02</b>

Напиши 2 функции:

<b>1.</b> <code>int calcFlightTime(double distance, double speed)</code>
   → возвращает время полёта в секундах (distance / speed)

<b>2.</b> <code>void emergencyCheck(float battery, int altitude)</code>
   → если <code>battery &lt; 15</code> ИЛИ <code>altitude &gt; 400</code>
   → выводит <b>"АВАРИЙНЫЙ РЕЖИМ"</b>
   → иначе — <b>"Параметры в норме"</b>

Вызови обе функции в <code>main()</code> 👇""",
            "check_prompt": "Проверь решение задания по функциям C++ для системы БПЛА. Задание: написать calcFlightTime (возвращает время) и emergencyCheck (проверка батареи/высоты). Дай развёрнутый фидбек на русском. Ответ пользователя:\n\n"
        },
        "ai_context": "Ты преподаватель C++ для разработчиков бортового ПО БПЛА. Объясняешь функции и их применение. Отвечай на русском."
    },

    "03": {
        "title": "ООП / Классы",
        "emoji": "🏗️",
        "desc": "Объектная модель — описываем реальные объекты",
        "lessons": {
            "1": {
                "title": "Класс и объект",
                "text": """🏗️ <b>Классы — основа C++ в ОПК</b>

<b>Класс = чертёж. Объект = изделие по чертежу.</b>

<pre><code>class Drone {
public:
    string id;
    int    altitude;
    double speed;
    float  battery;
    bool   isFlying;

    void takeoff() {
        isFlying = true;
        altitude = 50;
        cout &lt;&lt; id &lt;&lt; " взлетел!" &lt;&lt; endl;
    }

    void report() {
        cout &lt;&lt; "[" &lt;&lt; id &lt;&lt; "] H=" &lt;&lt; altitude
             &lt;&lt; "м V=" &lt;&lt; speed &lt;&lt; "м/с" &lt;&lt; endl;
    }
};

int main() {
    Drone uav1;
    uav1.id = "UAV-001";
    uav1.battery = 95.0f;
    uav1.speed = 22.5;
    uav1.takeoff();
    uav1.report();
}</code></pre>"""
            },
            "2": {
                "title": "Конструктор и инкапсуляция",
                "text": """🔒 <b>Конструктор и инкапсуляция</b>

<pre><code>class Sensor {
private:              // снаружи недоступно
    double value;
    string sensorType;

public:
    // Конструктор — вызывается при создании
    Sensor(string type, double initVal) {
        sensorType = type;
        value = initVal;
    }

    void update(double newVal) { value = newVal; }
    double read() const { return value; }

    void print() {
        cout &lt;&lt; sensorType &lt;&lt; ": " &lt;&lt; value &lt;&lt; endl;
    }
};

int main() {
    Sensor gyro("Гироскоп", 0.0);
    gyro.update(3.14);
    gyro.print();  // Гироскоп: 3.14
}</code></pre>

🔒 <b>Инкапсуляция:</b>
• <code>private:</code> — доступно только внутри класса
• <code>public:</code> — доступно снаружи
Никто не изменит показания датчика в обход твоей логики."""
            },
        },
        "task": {
            "text": """🎯 <b>Задание 03</b>

Создай класс <code>FlightController</code>:

<b>Поля (private):</b>
• <code>targetAltitude</code> (int) — целевая высота
• <code>currentAltitude</code> (int) — текущая высота
• <code>mode</code> (string) — режим: "HOLD" / "CLIMB" / "DESCEND"

<b>Методы (public):</b>
• Конструктор с параметрами
• <code>setTarget(int alt)</code> — устанавливает цель
• <code>update()</code> — сравнивает current и target, выставляет mode
• <code>printState()</code> — выводит все значения

👇""",
            "check_prompt": "Проверь решение задания по C++ ООП. Задание: класс FlightController с private полями и методами управления высотой БПЛА. Дай развёрнутый фидбек на русском. Ответ пользователя:\n\n"
        },
        "ai_context": "Ты преподаватель C++ ООП для разработчиков встроенных систем. Объясняй через примеры БПЛА и датчиков. Отвечай на русском."
    },

    "04": {
        "title": "Память / Указатели",
        "emoji": "🧠",
        "desc": "Самое важное в C++ — управление памятью",
        "lessons": {
            "1": {
                "title": "Что такое указатель",
                "text": """🧠 <b>Указатели — адреса в памяти</b>

Каждая переменная хранится по определённому адресу. Указатель хранит <b>адрес другой переменной</b>.

В embedded — ты буквально работаешь с регистрами железа по адресам.

<pre><code>int altitude = 200;
int* ptr = &amp;altitude;  // ptr = адрес переменной
                       // &amp; — оператор "взять адрес"

cout &lt;&lt; altitude &lt;&lt; endl;  // 200 — значение
cout &lt;&lt; ptr      &lt;&lt; endl;  // 0x7fff... — адрес
cout &lt;&lt; *ptr     &lt;&lt; endl;  // 200 — значение по адресу

*ptr = 350;               // меняем через указатель
cout &lt;&lt; altitude &lt;&lt; endl; // 350 (!)</code></pre>

💡 <b>Ключевые операторы:</b>
<code>&amp;</code> — взять адрес переменной
<code>*</code> при объявлении — «это указатель»
<code>*</code> при использовании — «значение по адресу»"""
            },
            "2": {
                "title": "Динамическая память",
                "text": """⚡ <b>Динамическая память — new / delete</b>

<pre><code>int* sensorData = new int;  // выделяем память
*sensorData = 42;
cout &lt;&lt; *sensorData &lt;&lt; endl;  // 42
delete sensorData;             // ОБЯЗАТЕЛЬНО!
sensorData = nullptr;          // обнуляем указатель

// Динамический массив телеметрии
int packetSize = 128;
float* telemetry = new float[packetSize];

for (int i = 0; i &lt; packetSize; i++) {
    telemetry[i] = 0.0f;
}
// ... обработка данных ...
delete[] telemetry;  // для массива — delete[]</code></pre>

⛔ <b>Утечка памяти — критично для БПЛА:</b>
Бортовой компьютер работает <b>часами</b>. Если не вызывать delete — память заканчивается, система падает прямо в полёте. В ОПК это называется «деградация ПО»."""
            },
        },
        "task": {
            "text": """🎯 <b>Задание 04</b>

<b>1.</b> Найди ошибку в этом коде и объясни почему это плохо:
<pre><code>int* p = new int(10);
p = new int(20);
delete p;</code></pre>

<b>2.</b> Напиши код:
• Создай динамический массив из 4 <code>float</code>
• Заполни значениями: 1.1, 2.2, 3.3, 4.4
• Выведи все элементы в цикле
• Корректно освободи память

👇""",
            "check_prompt": "Проверь ответы на задание по указателям C++. Задание 1: найти утечку памяти в коде. Задание 2: создать/заполнить/вывести/освободить динамический массив. Дай развёрнутый фидбек на русском. Ответ пользователя:\n\n"
        },
        "ai_context": "Ты эксперт по C++ для embedded-систем и бортового ПО. Объясняешь указатели и память через примеры БПЛА. Предупреждаешь об опасных паттернах. Отвечай на русском."
    },

    "05": {
        "title": "Linux & Инструменты",
        "emoji": "🐧",
        "desc": "Терминал, g++, CMake, git — рабочая среда ОПК",
        "lessons": {
            "1": {
                "title": "Базовые команды Linux",
                "text": """🐧 <b>Linux — стандарт для БПЛА</b>

Бортовые компьютеры (Nvidia Jetson, RPi, кастомные платы) — все под Linux. ROS/ROS2 работает только на Linux.

<b>Базовые команды — выучи наизусть:</b>
<pre><code>ls              # список файлов
ls -la          # подробный с правами
cd /home/user   # перейти в папку
cd ..           # на уровень выше
pwd             # текущая папка
mkdir src       # создать папку
rm file.cpp     # удалить файл
cat main.cpp    # показать файл
nano main.cpp   # редактировать файл
grep "error" log.txt  # найти в файле</code></pre>"""
            },
            "2": {
                "title": "Компиляция и CMake",
                "text": """⚙️ <b>Компиляция C++ в Linux</b>

<pre><code># Базовая компиляция
g++ main.cpp -o drone_control

# C++17 + оптимизация + все предупреждения
g++ -std=c++17 -O2 -Wall -Wextra main.cpp -o drone

# Несколько файлов
g++ -std=c++17 main.cpp drone.cpp sensor.cpp -o system

# Запуск
./drone_control</code></pre>

<b>CMakeLists.txt — реальный стандарт в ОПК:</b>
<pre><code>cmake_minimum_required(VERSION 3.10)
project(DroneControl)
set(CMAKE_CXX_STANDARD 17)

add_executable(drone_control
    main.cpp
    drone.cpp
    sensor.cpp
)
# mkdir build &amp;&amp; cd build
# cmake .. &amp;&amp; make</code></pre>

📁 <b>Git — основные команды:</b>
<pre><code>git init              # создать репозиторий
git add .             # добавить все файлы
git commit -m "msg"   # сохранить изменения
git push              # отправить на сервер
git clone URL         # скачать проект</code></pre>"""
            },
        },
        "task": {
            "text": """🎯 <b>Задание 05</b>

<b>1.</b> Напиши команду: скомпилировать <code>control.cpp</code> со стандартом C++17 и всеми предупреждениями, вывод назвать <code>control</code>

<b>2.</b> Как запустить получившийся файл в Linux?

<b>3.</b> Что делает флаг <code>-O2</code> у компилятора?

<b>4.</b> Напиши 3 git-команды:
добавить все файлы → коммит с сообщением → отправить на сервер

👇""",
            "check_prompt": "Проверь ответы по компиляции C++ в Linux и git. Вопросы: команда g++ с флагами C++17, запуск файла, флаг O2, последовательность git add/commit/push. Дай развёрнутый фидбек на русском. Ответ пользователя:\n\n"
        },
        "ai_context": "Ты эксперт по Linux и инструментам разработки для embedded C++ и БПЛА. Объясняешь команды терминала, g++, CMake, git. Отвечай на русском."
    },
}
